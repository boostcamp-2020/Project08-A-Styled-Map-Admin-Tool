import { stylingProps } from './index';

function administrativeStyling({
  map,
  subFeatureName,
  detailName,
  subDetailName,
  style,
}: stylingProps): void {
  console.log(1);
}

export default administrativeStyling;
