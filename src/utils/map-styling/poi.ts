import { stylingProps } from './index';

function poiStyling({
  map,
  subFeatureName,
  detailName,
  subDetailName,
  style,
}: stylingProps): void {
  console.log(1);
}

export default poiStyling;
