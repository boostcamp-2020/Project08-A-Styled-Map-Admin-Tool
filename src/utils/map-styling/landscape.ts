import { stylingProps } from './index';

function landscapeStyling({
  map,
  subFeatureName,
  detailName,
  subDetailName,
}: stylingProps): void {
  console.log(1);
}

export default landscapeStyling;
