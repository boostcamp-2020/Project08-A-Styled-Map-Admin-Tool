import { stylingProps } from '.';

function transitStyling({
  map,
  subFeatureName,
  detailName,
  subDetailName,
  style,
}: stylingProps): void {
  console.log(1);
}

export default transitStyling;
