import { stylingProps } from './index';

function markerStyling({
  map,
  subFeatureName,
  detailName,
  subDetailName,
  style,
}: stylingProps): void {
  console.log(1);
}

export default markerStyling;
